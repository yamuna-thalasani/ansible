- name: file variables
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print variables
    vars:
      COURSE: Ansible
    ansible.builtin.debug:
      msg: "Course is: {{ COURSE }}, Trainer is: {{ TRAINER }}"